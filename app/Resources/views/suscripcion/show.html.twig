{% extends 'base.html.twig' %}

{% block body %}
    <h1>Suscripción #{{ suscripcion.id }}</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ suscripcion.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ suscripcion.nombre }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ suscripcion.email }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('dash_suscripcion_index') }}">Regresar a la lista</a>
        </li>
        <li>
            <a href="{{ path('dash_suscripcion_edit', { 'id': suscripcion.id }) }}">Editar</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Eliminar">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
