{% extends "AppBundle:Dash:base.html.twig" %}
{% block title %}Personas{% endblock %}

{% block body %}
    <h1>Persona creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('dash_persona_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
