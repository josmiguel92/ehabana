{% extends "AppBundle:Dash:base.html.twig" %}
{% block title %}Carteles{% endblock %}

{% block body %}
    <h1>Crear Cartel</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-primary" value="Crear" />
    {{ form_end(form) }}

    <ul class="inline">
        <li>
            <a  class="btn btn-default" href="{{ path('dash_cartel_index') }}">Regresar a la lista</a>
        </li>
    </ul>
{% endblock %}
